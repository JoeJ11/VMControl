<p>您的用户尚未分组，请加入现有分组</p>
<% @user_groups.each do |group| %>
  <%= link_to group.name, join_user_group_url(group.id)  %>
  <p> 现有组员： </p>
  <% group.students.each do |s| %>
    <p><%= s.mail_address %></p>
  <% end %>
<% end %>
